<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top">
  <div class="container">
    <a class="navbar-brand fw-bold text-primary" (click)="scrollTo('home')" role="button">Ahmed Portfolio</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMain">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-1">
        <li class="nav-item">
          <a class="nav-link" [class.active]="currentSection === 'home'" (click)="scrollTo('home')" role="button">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" [class.active]="currentSection === 'projects'" (click)="scrollTo('projects')" role="button">Projects</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" [class.active]="currentSection === 'skills'" (click)="scrollTo('skills')" role="button">Skills</a>
        </li>
        <li class="nav-item d-lg-none">
          <a class="nav-link" [class.active]="currentSection === 'contact'" (click)="scrollTo('contact')" role="button">Contact</a>
        </li>
        <li class="nav-item d-none d-lg-block">
          <a class="btn btn-primary ms-3" [class.active]="currentSection === 'contact'" (click)="scrollTo('contact')" role="button">Contact</a>
        </li>
      </ul>
    </div>
  </div>
</nav>